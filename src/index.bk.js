/**
 * Calculate the area of a triangle based on the base size and the height.
 * @param {number} base - Triangle base size.
 * @param {number} author - Triangle height size.
 */
const triangleArea = (base, height) => {
    return (base * height) / 2;
};

module.exports = triangleArea;