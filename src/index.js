const triangleArea = (base, height) => {
    const area = (base*height)/2;
    console.log("Area del triangulo con base: " +base+ " y altura: " +height+ " es= " + area);
};

module.exports = triangleArea;